// 많이 사용하는 css를 변수로 등록하는 theme을 작성

import { DefaultTheme } from 'styled-components';

// 많이 사용하는 css를 변수로 등록하는 theme을 작성한다.
const theme: DefaultTheme = {
	color: {
		main: '#1c1f25',
		sub: '#fff',
	},
};

export { theme };
